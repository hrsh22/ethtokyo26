/** Installs the published agent toolkit (SDK, CLI and MCP server). Shared by server and client pages. */
export const agentInstall = "npm install https://accord.hrsh.dev/downloads/accord-agent-0.1.0.tgz";
