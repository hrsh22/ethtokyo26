# Repository instructions

When making commits in this repository, use `harsh <harsh.gupta2211@gmail.com>`
for both author and committer. Do not override Git's configured identity with
`Codex <codex@openai.com>`. Before committing, check `git var GIT_AUTHOR_IDENT`
and `git var GIT_COMMITTER_IDENT`.
